streamlit>=1.32.0
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
xgboost>=2.0.0
joblib>=1.3.0
plotly>=5.18.0
yfinance>=0.2.30
requests>=2.31.0
# Financial sentiment (FinBERT). The `transformers` + `torch` stack is the
# primary model; if it cannot load, the pipeline falls back to a finance
# lexicon classifier that needs no network.
transformers>=4.30.0
torch>=2.0.0
# Legacy/lexicon fallback kept for environments without torch/transformers.
textblob>=0.17.1
nltk>=3.8.1
# Development
pytest>=7.4.0
pytest-cov>=4.1.0
ruff>=0.3.0
black>=23.7.0